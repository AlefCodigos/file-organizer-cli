# File Organizer CLI

A command-line tool to organize files in a directory into folders based on file type.
By default, it runs in dry-run mode and never deletes files.

## Features (v1)
- Organize files by type (images, documents, videos, etc.)
- Dry-run mode by default
- Safe file moves (no deletion, no overwrite)

## Usage
Coming soon.